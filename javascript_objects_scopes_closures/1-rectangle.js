#!/usr/bin/node

class Rectangle {
  // Constructor to initiate the rectangle with width and height
  constructor (w, h) {
    this.width = w; // Set the width attribute
    this.height = h; // Set the height attribute
  }
}

module.exports = Rectangle;
